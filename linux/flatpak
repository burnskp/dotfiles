flathub=(
app.zen_browser.zen
com.discordapp.Discord
com.fastmail.Fastmail
com.github.tchx84.Flatseal
com.visualstudio.code
)

for pkg in ${flathub[@]}; do
  if flatpak info "$pkg" >/dev/null 2>&1; then
    flatpak update "$pkg"
  else
    flatpak install flathub "$pkg"
  fi
done
